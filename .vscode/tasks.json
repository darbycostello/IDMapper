{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pack",
            "type": "shell",
            "options": {"cwd": "${workspaceFolder}/code"},
            "command": "(rm -f \"idmapper.zip\"; zip -vr idmapper.zip idmapper -x \*.pyc)"
        }
    ]
}